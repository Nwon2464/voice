"""Minimal Windows-native audio bridge used from WSL.

This package deliberately contains no application, hotkey, or transcription
integration.  It only proves the Windows WASAPI loopback audio boundary.
"""
